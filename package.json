{
  "name": "deep-rename-keys-ts",
  "description": "Recursively rename the keys in an object.",
  "version": "1.0.1",
  "homepage": "https://github.com/Bullrich/deep-rename-keys-ts",
  "author": "Javier Bullrich (https://github.com/Bullrich)",
  "repository": "Bullrich/deep-rename-keys-ts",
  "bugs": {
    "url": "https://github.com/Bullrich/deep-rename-keys-ts/issues"
  },
  "license": "MIT",
  "main": "build/index.js",
  "scripts": {
    "test": "jest src/",
    "build": "rm -rf build && tsc"
  },
  "dependencies": {
    "isobject": "^4.0.0"
  },
  "devDependencies": {
    "@types/jest": "^27.5.0",
    "jest": "^28.0.3",
    "ts-jest": "^28.0.0",
    "typescript": "^4.6.4"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  },
  "keywords": [
    "keys",
    "nested",
    "object",
    "properties",
    "props",
    "recurse",
    "recursively",
    "rename"
  ]
}
